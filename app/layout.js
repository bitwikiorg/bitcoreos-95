import './globals.css';

export const metadata = {
  title: 'BITCOREOS-95',
  description: 'A semantic navigator for BIThub and BITwiki.'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
